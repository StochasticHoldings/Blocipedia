<h1>Edit wiki</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidlines For Editing Fishing Knowledge</p>
     <ul>
       <li>Make sure its on topic</li>
       <li> Make sure you have a picture or a video</li>
       <li>Make sure to use a Header 2 Tag</li>
     </ul>
   </div>
   <div class="col-md-8">
       <%= form_for @wiki do |w| %>
         <div class="form-group">
           <%= w.label :title %>
           <%= w.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
         </div>
         <div class="form-group">
           <%= w.label :body %>
           <%= w.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
         </div>
         <div class="form-group">
           <%= w.submit "Save", class: 'btn btn-success' %>
         </div>
       <% end %>
     </div>
   </div>
